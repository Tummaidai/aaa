import json
import time
import requests
import streamlit as st
from streamlit_lottie import st_lottie
from streamlit_lottie import st_lottie_spinner



st.header("ทฤษฎีที่เกี่ยวข้อง")


st.subheader("1.ทฤษฏี visualization")
st.info("""
        คือการเอาข้อมูลหรือ Data ที่มีความซับซ้อนมานำเสนอให้เข้าใจง่ายขึ้นผ่านแผนภูมิรูปภาพ แผนที่ กราฟ ตาราง วิดีโอ อินโฟกราฟิก (Infographic) แดชบอร์ด (Dashboard) ฯลฯ ทั้งนี้จะต้องมีการเลือกรูปแบบของสื่อให้เหมาะสมข้อมูลที่ต้องการนำเสนอ
        <br>
        
        """)